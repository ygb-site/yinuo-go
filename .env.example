# 一诺未来学堂 (YiNuo) 环境变量模板
#
# 使用方式：复制本文件为 .env，再填入自己的值。
# .env 已被 .gitignore 排除，只有本模板会进入版本控制。
#
# 安全约定（务必遵守）：
# 1. 本文件只允许出现变量名与占位值，禁止写入任何真实密钥。
# 2. 只有 VITE_ 前缀的变量会被打进前端产物，等同于公开信息。
#    因此严禁把 service_role key、数据库密码、第三方模型 API Key（sk-* 等）
#    以 VITE_ 前缀写进来——那等于直接发布到公网。
# 3. 第三方大模型 API Key 一律由用户在运行时输入，只存活于内存，
#    不进环境变量、不进浏览器存储、不进云端（详见 SECURITY.md）。

# Supabase 项目地址
# 获取路径：https://supabase.com -> 你的项目 -> Project Settings -> API -> Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon / publishable key（可公开，受 RLS 行级安全策略约束）
# 获取路径：Project Settings -> API -> anon public
# 注意：这里只能填 anon key，不要填 service_role key。
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
